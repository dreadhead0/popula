{
  "name": "popula",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack demographic analytics workspace for CSV ingestion, visual querying, and real-time visualization.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w backend",
    "test": "npm run test -w frontend && npm run test -w backend",
    "test:e2e": "npm run test:e2e -w frontend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:rollback": "npm run db:rollback -w backend",
    "db:status": "npm run db:status -w backend",
    "db:seed": "npm run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}